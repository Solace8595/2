@{
    ViewData["Title"] = "Главная";
}

<main class="text-center my-5">
    <div class="text-center mb-5">
        <img src="~/img/Круги.png" alt="Композиция" class="combined-photo" style="max-width: 100%; height: auto;" />
    </div>

    <div class="mt-4 text-start">
        <p>Современный паста-бар в историческом сердце города – Ленина, 44, старокирпичный переулок.</p>
        <p>В центре меню — паста собственного приготовления, лёгкие закуски и авторские коктейли, а также широкая карта вин.</p>
        <p>Также в нашем заведении есть бакалейная лавка, в которой вы всегда сможете приобрести свежую итальянскую пасту собственного приготовления.</p>
    </div>

    <div class="container mt-5">
        <div id="carouselExample" class="carousel slide" data-bs-ride="carousel">
            <div class="carousel-inner rounded shadow">
                @for (int i = 1; i <= 5; i++)
                {
                    <div class="carousel-item @(i == 1 ? "active" : "")">
                        <img src="@Url.Content($"~/img/Карусель/карусель{i}.jpg")" class="d-block w-100" alt="Карусель @i" style="max-height: 600px; object-fit: cover;" />
                    </div>
                }
            </div>

            <button class="carousel-control-prev" type="button" data-bs-target="#carouselExample" data-bs-slide="prev" aria-label="Предыдущий слайд">
                <span class="carousel-control-prev-icon" aria-hidden="true"></span>
            </button>
            <button class="carousel-control-next" type="button" data-bs-target="#carouselExample" data-bs-slide="next" aria-label="Следующий слайд">
                <span class="carousel-control-next-icon" aria-hidden="true"></span>
            </button>
        </div>
    </div>

        <div class="modal fade" id="imageModal" tabindex="-1" aria-hidden="true">
        <div class="modal-dialog modal-dialog-centered modal-lg">
            <div class="modal-content bg-transparent border-0">
                <div class="modal-body p-0 d-flex justify-content-center align-items-center" style="min-height: 300px;">
                    <img id="modalImage" src="" class="img-fluid rounded shadow" style="max-width: 90vw; max-height: 80vh; object-fit: contain;" />
                </div>
            </div>
        </div>
    </div>
</main>

@section Scripts {
    <script>
        const imageModal = document.getElementById('imageModal');
        imageModal.addEventListener('show.bs.modal', function (event) {
            const triggerImg = event.relatedTarget;
            const imgSrc = triggerImg.getAttribute('data-img');
            const modalImg = document.getElementById('modalImage');
            modalImg.setAttribute('src', imgSrc);
        });
    </script>
}
